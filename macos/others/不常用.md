

```shell

brew install ffmpeg && 
brew install smartmontools && 
brew install --cask disk-inventory-x && # 看檔案容量分布
brew install firebase-cli &&
brew install --cask affinity && # 修圖軟體
brew install --cask antigravity
# 檢查 SSD 健康度用的，`smartctl -a disk3s1s1` (後面的代號換成要查詢的 SSD 的裝置 ID)
# 裝置ID查詢方式：command + space 搜尋「磁碟工具程式」，看「裝置」的欄位
# 結果：percentage used 越低越好

# brew ls --versions anydesk || brew install --cask anydesk && 
# brew ls --versions typora || brew install --cask typora && 
# brew ls --versions intellij-idea || brew install --cask intellij-idea && 
# brew ls --versions microsoft-excel || brew install --cask microsoft-excel && 
# brew ls --versions android-file-trans || brew install --cask android-file-transfer && 
# brew ls --versions yt-music || brew install --cask yt-music && 
# brew ls --versions pgadmin4 || brew install --cask pgadmin4 && 
# brew ls --versions macdow || brew install --cask macdown &&
# brew ls --versions dash || brew install --cask dash &&
# brew ls --versions go2shell || brew install --cask go2shell && 
# brew ls --versions atom || brew install --cask atom && 
# brew ls --versions youtube-dl || brew install youtube-dl && 
# brew ls --versions ffmpeg || brew install ffmpeg && 
# brew ls --versions netbean || brew install --cask netbeans &&
# brew ls --versions toweR || brew install --cask tower && 

```
